Simulate email sending pp - Project scope:

* If any field is empty the user cannot send the email.
* Validate the Form Fields.
* When submitting the form an spinner(Image) should be displayed. 
* A Second spinner(Image) must be displayed after the email is sent and clear the form.
* A Reset Button should be added.

Please Note: The main source code contains in app.js.